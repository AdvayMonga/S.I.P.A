# Secrets / env
.env
.env.*
!.env.example

# Runtime data (VISION.md §6). Mostly derived/rebuildable (index.db, vault_search.db,
# scheduler_state.json), EXCEPT memory.db — the memory store is source-of-truth, local-only,
# no remote backup (design/memory-server.md). Gitignored for privacy; relies on local backup.
data/

# Tauri desktop app
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/node_modules/
desktop/dist/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual envs / uv
.venv/
venv/
.python-version

# Test / type / lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
.coverage
coverage.xml
htmlcov/

# Editors / OS
.idea/
.vscode/
.DS_Store
